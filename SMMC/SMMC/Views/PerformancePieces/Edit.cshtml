@model SMMC.Models.PerformancePiece

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>PerformancePiece</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PerformancePieceId" />
            <div class="form-group">
                <label asp-for="PerformanceId" class="control-label"></label>
                <select asp-for="PerformanceId" class="form-control" asp-items="ViewBag.PerformanceId"></select>
                <span asp-validation-for="PerformanceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PieceId" class="control-label"></label>
                <select asp-for="PieceId" class="form-control" asp-items="ViewBag.PieceId"></select>
                <span asp-validation-for="PieceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
