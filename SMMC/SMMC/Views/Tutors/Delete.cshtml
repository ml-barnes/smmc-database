@model SMMC.Models.Tutor

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>
<div>
    <h4>Tutor</h4>
    <hr />
    <p>Are you sure you want to delete @Html.DisplayFor(model => model.Staff.Person.FirstName) @Html.DisplayFor(model => model.Staff.Person.LastName) as Tutor?</p>

    <form asp-action="Delete">
        <input type="hidden" asp-for="TutorId" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
