@model SMMC.Models.EnsemblePerformance

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>EnsemblePerformance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EnsembleId" class="control-label"></label>
                <select asp-for="EnsembleId" class ="form-control" asp-items="ViewBag.EnsembleId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PerformanceId" class="control-label"></label>
                <select asp-for="PerformanceId" class ="form-control" asp-items="ViewBag.PerformanceId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
