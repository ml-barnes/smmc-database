@model IEnumerable<SMMC.Models.EnsemblePerformance>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ensemble)
            </th>
            <th>
                Venue
            </th>
            <th>
                Date
            </th>
            <th>
                Time
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ensemble.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Performance.Venue.VenueName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Performance.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Performance.Time)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
